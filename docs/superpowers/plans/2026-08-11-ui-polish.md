# UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Improve reward, question, hint, and dialogue presentation without changing game flow.

**Architecture:** Keep existing scene structure. Adjust `main.tscn` layout properties and small script behavior in `main.gd`, `question_box.gd`, and `dialog_box.gd`.

**Tech Stack:** Godot 4, GDScript, `.tscn` scene resources.

---

### Task 1: Reward and Hint Layout

**Files:**
- Modify: `scenes/main.tscn`
- Modify: `scripts/main.gd`

- [ ] **Step 1: Move reward pin down**

Set `PinImage` offsets to keep the 360x360 pin centered and lower than the text.

- [ ] **Step 2: Remove interaction hint display**

Keep interaction logic intact but make `show_npc_hint()` return without showing text.

### Task 2: Question Readability

**Files:**
- Modify: `scenes/main.tscn`
- Modify: `scripts/question_box.gd`

- [ ] **Step 1: Reduce scene font sizes**

Lower question font and answer label font sizes in `main.tscn`.

- [ ] **Step 2: Reduce runtime answer font default**

Lower `answer_label_font_size` default in `question_box.gd`.

### Task 3: Dialogue Portrait Polish

**Files:**
- Modify: `scenes/main.tscn`
- Modify: `scripts/dialog_box.gd`

- [ ] **Step 1: Normalize portrait size behavior**

Use aspect-centered portrait stretching in the scene.

- [ ] **Step 2: Add pose transition**

When portrait texture changes, fade/scale it briefly before showing the new pose.

### Task 4: Verify

**Files:**
- Check: modified files

- [ ] **Step 1: Run whitespace check**

Run: `git diff --check -- scenes/main.tscn scripts/main.gd scripts/question_box.gd scripts/dialog_box.gd`

Expected: no output.

- [ ] **Step 2: Try Godot headless if available**

Run: `godot --headless --path /home/newt/jogo-hub --quit`

Expected: command succeeds. If unavailable, report it.

## Self-Review

Spec coverage: covers pin position, hint removal, smaller question text, normalized portraits, and portrait transitions.

Placeholder scan: no placeholders remain.

Type consistency: no new external APIs introduced.
